---
title: Usage
sidebar_label: Usage
---

import ArtifactHubBadge from '@site/src/components/ArtifactHubBadge';

<ArtifactHubBadge name="component-chart" />

## Install Chart

### From Helm Repository

Add the Helm repository:
```bash
helm repo add helm-charts https://rishang.github.io/helm-charts/charts
helm repo update
```

### Create a `values.yaml`

```yaml
# values.yaml
containers:
  - name: demo
    image: nginx
    imageTag: "latest"

service:
  ports:
    - port: 80
```

Install or upgrade the chart:
```bash
helm upgrade --install my-component helm-charts/component-chart --values values.yaml
```

### From Local Path

For local installation:
```bash
helm install -n my-namespace my-component ./charts/component-chart/
```

## Upgrade Chart
```bash
helm upgrade -n my-namespace my-component ./charts/component-chart/
```

Or from repository:
```bash
helm upgrade my-component helm-charts/component-chart --values values.yaml
```

## Delete Chart
```bash
helm uninstall -n my-namespace my-component
```

## Use with DevSpace
To use the component chart with DevSpace CLI, add a helm chart deployment to `devspace.yaml` which defines `componentChart: true` or references the component chart using the `chart` options.
```yaml
deployments:
- name: my-component
  helm:
    componentChart: true
    values:
      containers:
      - name: mysql
        image: "dscr.io/username/mysql"
        volumeMounts:
        - containerPath: /var/lib/mysql
          volume:
            name: mysql-data
            subPath: /mysql
            readOnly: false
      volumes:
      - name: mysql-data
        size: "5Gi"
```

:::info
The `values` for the component chart can be defined in the `values` section of the deployment directly inside `devspace.yaml` (see example above).
:::

Deploy the chart using:
```bash
devspace deploy
```
